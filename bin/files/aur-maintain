#!/bin/bash

if [[ $# -lt 1 ]]; then
    echo 'Usage: aur-create pkg ...' >&2
    exit 1
fi

for pkg in $@; do
    echo "git clone ssh://aur@aur.archlinux.org/${pkg}.git"
    git clone ssh://aur@aur.archlinux.org/${pkg}.git
done

