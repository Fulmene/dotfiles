#!/bin/bash

if [[ $# -lt 1 ]]; then
    echo 'Usage: aur-fetch pkg ...' >&2
    exit 1
fi

for pkg in $@; do
    echo "git clone https://aur.archlinux.org/${pkg}.git"
    git clone https://aur.archlinux.org/${pkg}.git
done

