#!/bin/bash

options=\
"suspend
hibernate
logout
reboot
poweroff"
cmd=$(echo "$options" | rofi -dmenu -width 30 -lines 10 -p quit:)
case $cmd in
    logout)
        pkill xmonad
        ;;
    *)
        systemctl $cmd
        ;;
esac

