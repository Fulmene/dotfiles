#!/bin/sh

device=`nmcli dev | grep ' connected ' | awk '{print $2}'`

case $device in
    "ethernet")
        echo "Ethernet"
        ;;
    "wifi")
        iwgetid -r || nmcli -t -f active,ssid dev wifi | grep '^yes' | cut -d ':' -f 2 || echo "WiFi"
        ;;
    "")
        echo "<fc=#AF875F>Disconnect</fc>"
        ;;
esac

